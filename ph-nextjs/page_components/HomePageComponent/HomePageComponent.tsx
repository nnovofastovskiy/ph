import styles from "./HomePage.module.css";
import { HomePageComponentProps } from "./HomePageComponent.props";

export const HomePageComponent = ({ name }: HomePageComponentProps): JSX.Element => {
    return (
        <>
            HomePageComponent
        </>
    );
};